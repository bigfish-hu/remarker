machine:
  node:
    version: 6

dependencies:
  override:
    - cd admin
    - npm install

# Test Commands
test:
  override:
    - npm run build -e prod
    - npm run test
  post:
    - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js
